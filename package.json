{
  "name": "cms-mono",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "build:api": "npx lerna run --scope @edgefront/api build --stream",
    "run:cms-api": "npx lerna run --scope cms-api n --stream",
    "start:cms-api": "npm run bootstrap && npm run build:api && npm run build:cms-api && npx lerna run --scope cms-api start --stream",
    "build:cms-api": "npx lerna run --scope cms-api build --stream",
    "start": "npm run bootstrap && npm run build:api && npm run run:cms-api",
    "postinstall": "npm run bootstrap"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
