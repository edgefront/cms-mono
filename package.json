{
  "name": "tykoa-mono",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "build:api": "npx lerna run --scope @edgefront/api build --stream",
    "run:frontpage-api": "npx lerna run --scope tykoa n --stream",
    "start": "npm run bootstrap && npm run build:api && npm run run:frontpage-api",
    "postinstall": "npm run bootstrap"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
